function turn_toward_direction(argument0, argument1) {
	dir = argument0

	if direction-dir > 180 then dir = 360 + dir
	if dir-direction > 180 then dir = dir - 360

	if direction > dir then direction -= argument1
	if direction < dir direction += argument1
	my_angle=direction



}
