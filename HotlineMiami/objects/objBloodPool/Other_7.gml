image_speed = 0;
if (surface_exists(global.surf3)) {
    if (image_blend == c_aqua) {
        // do nothing
    } else {
        if (global.surfaces)
            addToSurface(global.surf3, 0);
    }
}

